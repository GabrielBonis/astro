# 🌌 Astro Project

Este projeto é uma aplicação web desenvolvida em **Python** focada na visualização e consulta de dados astronômicos. A aplicação utiliza o framework **Flask** e está configurada para execução serverless através da **Vercel**.

## 🚀 Tecnologias Utilizadas

* **Python**: Lógica de backend e processamento de dados.
* **Flask**: Micro-framework para gerenciamento de rotas e API.
* **Vercel**: Infraestrutura de hospedagem e deploy contínuo.

## 🛠️ Estrutura do Repositório

* `app.py`: Ponto de entrada da aplicação e definição das rotas.
* `requirements.txt`: Dependências do projeto (Flask, etc).
* `vercel.json`: Configurações de build e roteamento para o ambiente Vercel.
* `.gitignore`: Definição de arquivos que não devem ser versionados.

## ⚙️ Instalação e Execução Local

1.  **Clone o repositório:**
    ```bash
    git clone [https://github.com/GabrielBonis/astro.git](https://github.com/GabrielBonis/astro.git)
    cd astro
    ```

2.  **Crie e ative um ambiente virtual:**
    ```bash
    python -m venv venv
    # No Windows:
    .\venv\Scripts\activate
    # No Linux/Mac:
    source venv/bin/activate
    ```

3.  **Instale as dependências:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Execute a aplicação:**
    ```bash
    python app.py
    ```
    A aplicação estará disponível em `http://127.0.0.1:5000`.

## 🌐 Link do Projeto

A versão estável do projeto pode ser acessada em:  
👉 [astro-two-bice.vercel.app](https://astro-two-bice.vercel.app)

---
*Desenvolvido por [Gabriel Bonis](https://github.com/GabrielBonis).*